# Copy this file to .env and fill in your local values

# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGDATABASE=geopulse
PGUSER=postgres
PGPASSWORD=postgres

# Server
PORT=4000

# CORS — the URL your Vite client runs on
CLIENT_ORIGIN=http://localhost:5173
